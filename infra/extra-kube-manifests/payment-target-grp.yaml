apiVersion: gateway.k8s.aws/v1beta1
kind: TargetGroupConfiguration
metadata:
  name: payment-tg-config
  namespace: edulearn-app
spec:
  targetReference:
    name: edulearn-payment
  defaultConfiguration:
    targetType: ip
