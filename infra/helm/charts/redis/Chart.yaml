apiVersion: v2
name: redis
description: Redis wrapper using Bitnami chart
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: redis
    version: 22.x.x
    repository: https://charts.bitnami.com/bitnami
    alias: redis