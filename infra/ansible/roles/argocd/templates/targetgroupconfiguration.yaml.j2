apiVersion: gateway.k8s.aws/v1beta1

kind: TargetGroupConfiguration

metadata:

  name: {{ argocd_targetgroup_name }}

  namespace: {{ argocd_namespace }}

spec:

  targetReference:

    name: argo-cd-argocd-server

  defaultConfiguration:

    targetType: ip