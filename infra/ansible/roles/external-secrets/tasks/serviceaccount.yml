---

- name: Create External Secret Operator ServiceAccount
  kubernetes.core.k8s:

    state: present

    definition:

      apiVersion: v1

      kind: ServiceAccount

      metadata:

        name: "{{ external_secrets_sa }}"

        namespace: "{{ external_secrets_namespace }}"