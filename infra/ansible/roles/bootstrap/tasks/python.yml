- name: Install pip
  become: true
  apt:
    name:
      - python3-pip
    state: present
    update_cache: yes

- name: Install Kubernetes Python client
  become: true
  pip:
    name:
      - kubernetes
      - openshift
      - pyyaml
    executable: pip3