---
- name: Update package cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install base packages
  apt:
    name:
      - unzip
      - curl
      - git
      - jq
      - python3-pip
      - software-properties-common
    state: present
    update_cache: yes
