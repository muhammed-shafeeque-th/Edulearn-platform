---

- name: Create AWS Load Balancer Controller ServiceAccount
  kubernetes.core.k8s:

    state: present

    definition:

      apiVersion: v1

      kind: ServiceAccount

      metadata:

        name: "{{ aws_lbc_sa }}"

        namespace: "{{ aws_lbc_namespace }}"